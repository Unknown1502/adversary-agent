# Adversary runtime dependencies.
#
# Per the architecture decision (OQ-1): unpinned >= ranges for first install,
# then `pip freeze > requirements.lock` post Phase-1 smoke and commit the lock.
# This stack (google-adk, arize-phoenix, openinference-*) moves quickly enough
# that hand-picked pins go stale; trust the working environment, then freeze.

# --- Agent runtime ---
google-adk>=1.0
google-genai>=1.0

# --- Observability ---
arize-phoenix>=5.0
arize-phoenix-otel>=0.6
openinference-instrumentation-google-adk>=0.1
openinference-instrumentation-google-genai>=0.1
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.1
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27

# --- Test ---
pytest>=8.0
pytest-asyncio>=0.23
